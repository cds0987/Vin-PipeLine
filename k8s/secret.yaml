# secret.yaml — template only, do NOT commit real values
# Populate via: kubectl create secret generic vin-pipeline-secret --from-env-file=.env.gcp
apiVersion: v1
kind: Secret
metadata:
  name: vin-pipeline-secret
  namespace: default
type: Opaque
stringData:
  DATABASE_URL: "postgresql://rag:rag@postgres:5432/ragdb"
  QDRANT_API_KEY: ""
  AI_API_KEY: "sk-placeholder"
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
  S3_ENDPOINT: ""
  S3_BUCKET: ""
